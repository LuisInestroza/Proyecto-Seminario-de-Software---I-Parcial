<section id="three" class="wrapper style3 special">
    <div class="container">

    </div>
    <div class="container 50%">
        <form action="" method="post">

            <div class="acciones btnVerde">
                <a href="/insumo/{{url}}">Presupuesto</a>
                <a href="/crearInsumo">Nuevo Insumo</a>
            </div>

            <p id="total">Total Actual:</p>
            <table class="table table-hover" id="insumo">

                <thead>
                    <tr>
                        <td scope="col">Descripción</td>
                        <td scope="col">Fecha</td>
                        <td scope="col">Total</td>
                        <td scope="col">Acciones</td>

                    </tr>
                </thead>

                <tbody class="insumo">
                    {{#if insumos.length}}
                    {{#each insumos}}
                    <tr>

                        <td>{{descripcion}}</td>
                        <td>{{fecha}}</td>
                        <td>L. {{total}}</td>
                        <td>
                            <div class="contenedor">
                                <div class="caja">
                                    <a href="#" data-eliminar={{id}}>Eliminar</a>
                                    <a href="/insumo/{{url}}">Editar</a>

                                </div>
                            </div>

                        </td>
                    </tr>
                    {{/each}}
                    {{/if}}
                    <tr>
                        <td>Total</td>
                    </tr>

                </tbody>

            </table>

        </form>
    </div>
</section>