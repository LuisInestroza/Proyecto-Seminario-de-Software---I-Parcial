<section id="three" class="wrapper style3 special">
    <div class="container">

    </div>
    <div class="container 50%">
        <form action="" method="post">

            <div class="acciones btnVerde">
                <a href="/crearImporte">Presupuesto</a>
                <a href="/crearInsumo">Nuevo Gasto</a>
                <a href="/editarPresupuesto">Editar Presupuesto</a>
            </div>

            <table class="table table-hover" id="insumo">
                <div>
                    {{#if importe.length}}
                    {{#each importe}}
                    <label for="presupuesto">Limite de presupuesto:</label>
                    <p class="presupuesto">{{cantidad}}</p>
                    {{/each}}
                    {{/if}}
                </div>
                <thead>
                    <tr>
                        <td scope="col">Descripción</td>
                        <td scope="col">Fecha</td>
                        <td scope="col">Total</td>
                        <td scope="col">Acciones</td>

                    </tr>


                <tbody class="insumo">
                    {{#if insumo.length}}
                    {{#each insumo}}
                    <tr>

                        <td class="ColumnaA">{{descripcion}}</td>
                        <td class="ColumnaB">{{fecha}}</td>
                        <td class="ColumnaC">{{total}}</td>
                        <td>
                            <div class="contenedor">
                                <div class="caja">
                                    <a href="#" data-eliminar={{id}}>Eliminar</a>
                                    <a href="/editarInsumo/{{url}}">Editar</a>

                                </div>
                            </div>

                        </td>
                    </tr>
                    {{/each}}
                    {{/if}}
                    <tr>
                        <td class="Total">Total</td>
                        <td></td>
                        <td class="Total C"></td>

                    </tr>

                </tbody>
                </thead>

            </table>

        </form>
    </div>
</section>